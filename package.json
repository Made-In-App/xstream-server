{
  "name": "xstream-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-device Xtream replication and proxy platform",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "dev": "pnpm --filter api dev",
    "ingest": "pnpm --filter @xstream/ingest ingest",
    "test-env": "pnpm --filter @xstream/ingest test-env"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
